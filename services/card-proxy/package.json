{
  "name": "card-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxying Claude API card generation for the CITAItokens game, so the API key stays server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.4",
    "wrangler": "^4.116.0"
  }
}
